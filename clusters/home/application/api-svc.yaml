apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-svc
  name: api-svc
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9090
  selector:
    app: api-gateway
  type: ClusterIP
