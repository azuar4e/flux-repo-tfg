apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: api-gateway-tfg
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: api-gateway-tfg
  filterTags:
    pattern: '^\d+$'
  policy:
    numerical:
      order: asc
